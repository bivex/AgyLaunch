<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to list apps on Android 11+ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".LauncherApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProgramLauncher">

        <!-- Launcher Home Activity -->
        <activity
            android:name=".adapter.in.ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:clearTaskOnLaunch="true"
            android:stateNotNeeded="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- BroadcastReceiver for Programmatic ADB Control -->
        <receiver
            android:name=".adapter.in.receiver.LauncherReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.programlauncher.ADD_SHORTCUT" />
                <action android:name="com.example.programlauncher.REMOVE_SHORTCUT" />
                <action android:name="com.example.programlauncher.CLEAR_LAYOUT" />
                <action android:name="com.example.programlauncher.SET_GRID" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
